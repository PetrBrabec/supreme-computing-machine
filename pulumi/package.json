{
    "name": "supreme-computing-machine",
    "main": "index.ts",
    "scripts": {
        "build": "cd ../ && ./build.sh",
        "redeploy": "npm run build && pulumi destroy --target urn:pulumi:dev::pulum::hcloud:index/server:Server::supreme-computing --target-dependents --yes && pulumi up --yes --config-file Pulumi.dev.yaml",
        "redeploy-all": "npm run build && pulumi destroy --yes --config-file Pulumi.dev.yaml && pulumi up --yes --config-file Pulumi.dev.yaml",
        "refresh": "pulumi refresh --yes"
    },
    "devDependencies": {
        "@types/node": "^16",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@pulumi/command": "^1.0.1",
        "@pulumi/hcloud": "^1.21.0",
        "@pulumi/pulumi": "^3.113.0"
    }
}
